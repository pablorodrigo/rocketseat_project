import React from 'react';

import ReactDOM from 'react-dom'; // Allow React connect to elements tree
import App from './App'; //

ReactDOM.render(<App />, document.getElementById('root'));

// JSX = JavaScript+Html